{
  "id": "cafd8ef5-2c5e-415b-a0b9-eb777079a80b",
  "name": "Runner Extras - Standalone Suites (consolidated)",
  "publisher": "AL Runner",
  "version": "1.0.0.0",
  "brief": "Consolidates 16 previously-standalone runner-extras suites (no declared dependencies, no cross-suite hazard) into one app to cut per-bundle CI overhead. See #1847.",
  "description": "Each subfolder is one formerly-independent app.json bundle, kept as-is except for its app.json: allobj-virtual-table, allobjwithcaption-virtual-table, crypto-hash-instream, errormessages-page-setrecords, event-var-outstream, extension-object-metadata, field-virtual-table, media-non-image-content, query-join, report-metadata-virtual-table, report-run-execution, report-run-static-unresolvable, report-saveas-stream, table-metadata-virtual-table, task-scheduler-create-task, virtual-field-caption. Verified before merging: every object id/name is unique across the group, every idRange is disjoint, and the virtual-table suites (AllObj/AllObjWithCaption/Field/Table Metadata/Report Metadata/virtual-field-caption) filter every assertion to their own object ids rather than asserting a global count or an exhaustive list, so sharing one object inventory with the other 15 suites does not change what they prove. install-trigger-seed and install-trigger-text-constant were NOT merged in — see their own app.json comments and issue #1847 for why per-app install-trigger semantics make them stay isolated. xmlport-cluster-hooks-1800 (#1800) proves the NavXmlPort ctor-scaffolding/Run/Export/Import/SetTableView cluster, previously an orphaned JmpHook no-op, now actually fires via Cecil ownership. single-site-orphaned-hooks-1883 (#1883 follow-up to #1800/#1947) proves the 15-type 'single Hook() call site' cluster: 14 regression guards for orphaned JmpHook registrations whose call sites were deleted because BC's real, unpatched body was already correct (or already Cecil-owned by a separate mechanism), plus the one genuine bug in that cluster (NavHttpRequestMessage.get_Target), fixed by adding Cecil ownership. isolated-storage-1883 (#1883 follow-up, the 'ALIsolatedStorage 17-hook' cluster) proves the same pattern for the 17 orphaned ALIsolatedStorage.AL* JmpHook registrations deleted from TenantStoragePatches.cs: BC's real, unpatched bodies delegate entirely to IsolatedStorageRepository/ALSystemEncryption, both already Cecil-rewritten onto the SAME in-memory store/AES envelope, so the 17 JmpHooks were pure duplication, not a faithfulness gap. error-string-hooks-1883 (#1883 follow-up) proves the ALSystemErrorHandling (get_ALGetLastErrorText/Code/CallStack, ALClearLastError) and ALSystemString (ALLowercase, ALUppercase) clusters, both deleted because BC's real, unpatched bodies were already correct (the deleted registrations' 'reaches null NavCurrentThread.Session' rationale was stale — Session is already wired to the skeleton), plus a bonus cleanup of 3 further Hook(...) call sites (NavHttpClient/NavHttpRequestMessage/NavHttpResponseMessageBase get_Target) that were provably redundant (already Cecil-owned in every configuration, not just the default) rather than orphaned. nav-cancellation-token-1883 (#1883 follow-up) proves the NavCancellationToken (ThrowIfCancellationRequested, ThrowOperationCanceledException x2) cluster, deleted because BC's real, unpatched ThrowIfCancellationRequested body only reads a plain bool field (never the private 'source' field the deleted registration's 'uninitialized token trips the check' comment worried about) and the runner never threads a bare default token through the Find/FindSet/Next path anyway — NavSession.CancellationToken always resolves to a real, never-cancelled token, the same shape NclCecilRewrite.cs documents for report resource-governance's root-scope token. aldatabase-cluster-1883 (#1883 follow-up) proves 9 of ALDatabase's 15 orphaned registrations: ALSid, ALSessionID, get_/set_ALLockTimeout, get_/set_ALLockTimeoutDuration, and ALGetDefaultTableConnection/ALImportData were deleted because BC's real, unpatched bodies were already correct standalone (empirically probed against the un-hooked build, not just the deleted registrations' own 'NREs on session.Database/Session' comments — which turned out stale, same discipline as error-string-hooks-1883/nav-cancellation-token-1883), while get_ALSerialNumber is the one genuine bug in the cluster (NavSession.get_License() really does NRE) and is now Cecil-owned instead of JmpHook-owned. ALChangeUserPassword/ALSetUserPassword/ALDataFileInformation/ALAlterKeyAsync remain orphaned, deferred to a future #1883 slice.",
  "privacyStatement": "",
  "EULA": "",
  "help": "",
  "url": "",
  "logo": "",
  "dependencies": [],
  "screenshots": [],
  "platform": "1.0.0.0",
  "application": "27.0.0.0",
  "idRanges": [
    { "from": 60300, "to": 60399 },
    { "from": 60600, "to": 60699 },
    { "from": 60700, "to": 60799 },
    { "from": 60950, "to": 60999 },
    { "from": 61860, "to": 61869 },
    { "from": 61890, "to": 61899 },
    { "from": 61900, "to": 61909 },
    { "from": 61950, "to": 61969 },
    { "from": 61970, "to": 61989 },
    { "from": 62040, "to": 62049 },
    { "from": 62100, "to": 62119 },
    { "from": 62140, "to": 62149 },
    { "from": 62170, "to": 62179 },
    { "from": 62180, "to": 62189 },
    { "from": 62190, "to": 62199 },
    { "from": 62200, "to": 62209 },
    { "from": 64100, "to": 64199 },
    { "from": 64220, "to": 64249 },
    { "from": 64300, "to": 64309 }
  ],
  "runtime": "14.0",
  "features": []
}
