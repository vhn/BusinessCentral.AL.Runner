// A permission target for the IncludedPermissionSets chain, so those permission sets carry a
// `Permissions` line of their own. Never edited by any test.
codeunit 72158 "BN Perm Probe"
{
    procedure Value(): Integer
    begin
        exit(158);
    end;
}
