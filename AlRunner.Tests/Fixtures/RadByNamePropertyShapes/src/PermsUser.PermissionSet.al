// Permissions, W — the W this shape WOULD use: including the bystander is what would force its
// `Permissions` list to resolve out of the packaged baseline. No test drives it; see
// PermsHolder.PermissionSet.al.
permissionset 72162 "BN Perms User"
{
    Assignable = true;
    Caption = 'perms-user-v1';
    IncludedPermissionSets = "BN Perms Holder";
}
