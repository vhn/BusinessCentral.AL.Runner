// RoleCenter, V — the BYSTANDER. Untouched, so its serialized ProfileDefinition is what the
// delta resolves, and `RoleCenter` names the page by name.
profile "BN RoleCenter Profile"
{
    Caption = 'BN RoleCenter Profile';
    ProfileDescription = 'Bystander: its RoleCenter names a page the delta strips.';
    RoleCenter = "BN RoleCenter Page";
    Enabled = true;
}
